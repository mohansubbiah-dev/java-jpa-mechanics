# Default Profile - Uses Active Profile (local or prod)
# Set using: -Dspring.profiles.active=local or -Dspring.profiles.active=prod
# Or in IDE: Run Configuration > VM options > -Dspring.profiles.active=local

spring.application.name=hibernateqa
